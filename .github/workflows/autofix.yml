name: CI

on:
  pull_request:
  push:
    branches: ["main"]
  merge_group:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_INCREMENTAL: 0
  CARGO_TERM_COLOR: always
  COVERAGE_MIN_LINES: "80"
  COVERAGE_TARGET_LINES: "80"
  CORE_COVERAGE_TARGET_LINES: "90"
  MSRV: "1.97.1"
  RUSTUP_TOOLCHAIN: stable

jobs:
  fmt:
    name: Rustfmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all --check

  test:
    name: Test (${{ matrix.os }}, ${{ matrix.rust }})
    needs: fmt
    runs-on: ${{ matrix.os }}
    env:
      RUSTUP_TOOLCHAIN: ${{ matrix.rust }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            rust: stable
          - os: macos-latest
            rust: stable
          - os: windows-latest
            rust: stable
          - os: ubuntu-latest
            rust: "1.97.1"
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - name: Build
        run: cargo build --workspace --locked
      - name: Clippy
        run: cargo clippy --workspace --all-targets --all-features
      - name: Test
        run: cargo test --workspace --locked

  feature-gates:
    name: Feature gates
    needs: fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Build lean proxyapi without optional engines
        run: cargo build -p proxyapi --locked --no-default-features
      - name: Build without default features
        run: cargo build --workspace --locked --no-default-features
      - name: Test without default features
        run: cargo test --workspace --locked --no-default-features
      - name: Test proxyapi scripting explicitly
        run: cargo test -p proxyapi --locked --features scripting,vendored-lua -- scripting
      - name: Test proxyapi HTTP/3 explicitly
        run: cargo test -p proxyapi --locked --features http3

  coverage:
    name: Coverage
    needs: fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@cargo-llvm-cov
      - name: Coverage policy
        run: |
          echo "Blocking workspace gate: ${COVERAGE_MIN_LINES}% line coverage"
          echo "Blocking proxyapi gate: ${CORE_COVERAGE_TARGET_LINES}% line coverage"
      - name: Workspace coverage
        run: >
          cargo llvm-cov --workspace --all-features --locked
          --ignore-filename-regex '(^|/)(tests|target)/'
          --fail-under-lines "$COVERAGE_MIN_LINES"
      - name: Core proxyapi coverage
        run: >
          cargo llvm-cov report -p proxyapi
          --ignore-filename-regex '(^|/)(tests|target)/'
          --fail-under-lines "$CORE_COVERAGE_TARGET_LINES"

  package:
    name: Inspect crate packages
    needs: fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      # Stable Cargo resolves packaged path dependencies from crates.io. List
      # every archive here; the release job publishes the dependency layers in
      # order before packaging the next layer against the live index.
      - run: cargo package --workspace --list

  rustdoc:
    name: Rustdoc
    needs: fmt
    runs-on: ubuntu-latest
    env:
      RUSTDOCFLAGS: -D warnings
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo doc --workspace --all-features --no-deps --locked

  audit:
    name: Security audit
    runs-on: ubuntu-latest
    permissions:
      checks: write
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: rustsec/audit-check@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

  check:
    name: Required checks
    if: always()
    needs:
      - fmt
      - test
      - feature-gates
      - coverage
      - package
      - rustdoc
      - audit
    runs-on: ubuntu-latest
    steps:
      - name: Verify all required jobs passed
        run: |
          test "${{ needs.fmt.result }}" = "success"
          test "${{ needs.test.result }}" = "success"
          test "${{ needs.feature-gates.result }}" = "success"
          test "${{ needs.coverage.result }}" = "success"
          test "${{ needs.package.result }}" = "success"
          test "${{ needs.rustdoc.result }}" = "success"
          test "${{ needs.audit.result }}" = "success"
